<mvvm:BaseUserControl x:Class="Tuner.Dels.VUMeter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:mvvm="clr-namespace:Tuner.FrameworkMVVM"
             xmlns:local="clr-namespace:Tuner.Dels"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid x:Name="LayoutRoot">
        <Viewbox>
        <ItemsControl x:Name="DelListControl" ItemsSource="{Binding Dels}">

                <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel x:Name="DelListStackPanel" Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        </Viewbox>
    </Grid>

</mvvm:BaseUserControl>
